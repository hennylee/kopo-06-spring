<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member.MemberDAO" >
	<!-- 컬럼과 vo의 매핑코드 -->
	<resultMap type="kr.kro.globalpay.member.vo.MemberVO" id="memberMap">
		<result column="birth_date" property="birthDate"/>
		<result column="reg_date" property="regDate"/>
		<result column="privacy_agree" property="privacyAgree"/>
		<result column="openbank_agree" property="openbankAgree"/>
	</resultMap>

	<insert id="register" parameterType="memberVO">
		insert into member(
		    id, password, name, birth_date, gender, email, addr1, addr2, zip, phone, authority, type, privacy_agree, openbank_agree
		)values(
			#{id}, #{password}, #{name}, #{birthDate}, #{gender}, #{email}, #{addr1}, #{addr2}, #{zip}, #{phone}
			, #{authority}, #{type}, #{privacyAgree}, #{openbankAgree}
		);
	</insert>
	
	<!-- <select id="login" parameterType="memberVO" resultType="memberVO">
		select id, name, password, type
			from t_member
			where id = #{id} and password = #{password}
	</select>
 -->

</mapper>